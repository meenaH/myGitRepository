JavaScript has many values : booleans, numbers, strings, arrays, and so on. 

All values in JavaScript have Properties.

Each property has a key (or name) and a value. 

You can think of properties like fields of a record. Use the dot (.) operator to read a property: e.g. abc.length



